import { VoicesLayout } from "@/src/feature/voices/views/voices-layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <VoicesLayout>{children}</VoicesLayout>;
}
